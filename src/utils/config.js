// src/utils/config.js
export const CONFIG = {
  // ใช้ IP address ของเครื่องที่ run backend
  API_BASE_URL: 'http://192.168.1.40:5000/api', // เปลี่ยนเป็น IP จริงของคุณ
  
  // หรือใช้ localhost สำหรับ Android emulator
  // API_BASE_URL: 'http://10.0.2.2:5000/api', 
  
  // สำหรับ iOS simulator
  // API_BASE_URL: 'http://localhost:5000/api',
};